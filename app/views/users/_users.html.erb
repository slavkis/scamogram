<% users.each do |user| %>
  <ul>
    <%= link_to "#{user.name}", user_path(id: user.id) %><br />
    <% unless user == current_user %>
      <%= button_to "Send message", chats_path(recipient_id: user.id), method: :post, class: 'btn btn-outline-primary btn-sm' %>
    <% end %>
  </ul>
<% end %>